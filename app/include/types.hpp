#ifndef TYPES_HPP
#define TYPES_HPP

#include <Bounce.h>

struct Buttons {
  /* Bounce left;
  Bounce nav;
  Bounce select;
  Bounce right; */
  Bounce stop;
  Bounce record;
  Bounce play;
};

#endif
